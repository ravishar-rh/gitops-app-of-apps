apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - oadp-subscription.yaml
  - operatorgroup.yaml
  - subscription.yaml
  - multiclusterhub-patch.yaml
  - credentials-secret.yaml
  - dataprotectionapplication.yaml
  - backupschedule.yaml
  # Uncomment only when restoring to a new hub cluster:
  # - restore.yaml
